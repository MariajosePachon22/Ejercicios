#47.Encontrar el número más pequeño de una lista.
numeros = [8, 3, 12, 5, 1, 9]
minimo = numeros[0]
for num in numeros:
    if num < minimo:
        minimo = num

print("El número más pequeño es:", minimo)

















